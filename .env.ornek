# Örüntü Motoru — ortam değişkenleri örneği
#
# Bu dosyayı ".env" adıyla kopyalayıp kendi değerini yaz.
# .env dosyası .gitignore'dadır; depoya ASLA gitmez.
#
# Yerelde bu dosya ZORUNLU DEĞİLDİR: .env yoksa sunucu "uycep-local"
# varsayılanıyla açılır ve npm start tek başına yeter.
#
# Render'da bu dosya KULLANILMAZ; değeri Render panelinden
# Environment sekmesine gir. Tanımlanmazsa /teacher girişi kapalı kalır.

ADMIN_PASSWORD=kendi-sifren
